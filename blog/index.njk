---
layout: layout.njk
title: Blog
---

<h1>Blog</h1>

{% set postsOrdenados = collections.blogPosts | sort(attribute='data.date') | reverse %}

{% if postsOrdenados.length > 0 %}
  <ul>
    {% for post in postsOrdenados %}
      <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a> - {{ post.data.date | simpleDate }}
        {% if post.data.categories %}
          | Categorías: 
          {% for cat in post.data.categories %}
            <a href="/blog/categoria/{{ cat | slug }}/">{{ cat }}</a>{% if not loop.last %}, {% endif %}
          {% endfor %}
        {% endif %}
      </li>
    {% endfor %}
  </ul>
{% else %}
  <p>No hay posts aún.</p>
{% endif %}
