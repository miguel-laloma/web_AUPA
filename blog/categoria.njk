---
pagination:
  data: collections.blogCategories
  size: 1
  alias: categoria
permalink: "/blog/categoria/{{ categoria | slug }}/"
layout: layout.njk
title: "Categoría: {{ categoria }}"
---

<h1>Entradas en categoría "{{ categoria }}"</h1>

<ul>
  {% set postsFiltrados = [] %}
  {% for post in collections.blogPosts %}
    {% if post.data.categories and categoria in post.data.categories %}
      {% set postsFiltrados = postsFiltrados.concat([post]) %}
    {% endif %}
  {% endfor %}

  {% set postsOrdenados = postsFiltrados | sort(attribute='data.date') | reverse %}

  {% for post in postsOrdenados %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a> — {{ post.data.date | simpleDate }}
    </li>
  {% else %}
    <li>No hay entradas en esta categoría.</li>
  {% endfor %}
</ul>

<a href="/blog/">Volver al blog</a>
